# The verification contract

The harness drives a real consumer model against an MCP server under test, through a real driver (an agent harness such as Claude Code headless), and records what happened. Humans and the suite's spec session score the results against criteria pinned in advance. The harness is generic; everything server-specific arrives in a **suite** — a manifest plus its groundings and checks, owned by the server project's own repository (ruling S1, `00-INDEX.md`).

This file is the contract for the harness itself. The interface a suite must satisfy is `20-manifest.md`; the Layer-1 check language is `30-checks.md`. The ancestor of this design is congressMCP's §17 harness, preserved verbatim in `examples/congressmcp/` — historical claims below (the A3/B3/E3 invalidations, the zero-trace incident) refer to that record.

## Division of labor — binding

The harness **executes and records; it never scores.** Every prompt carries its `pass`/`fail` criteria pinned before the run — preregistration-of-scoring — and a criterion is never edited after seeing a result it would score. Criteria change only between runs, with the rationale committed in the suite repo. Scoring is done by a human and/or the suite's spec session, recorded beside (never instead of) the raw artifacts.

The manifest is normative and lives in the suite repo. The harness loads that file verbatim — no harness-side copy, no transcription, no defaults silently merged in. Manifest edits are suite-repo spec commits. **Applied 2026-09-18 to the loop driver's settling runs:** the Q8 runs used a harness-side derived copy of the uscde-mcp manifest with loop cells substituted, acceptable for settling the instrument (the runs measured the driver, not the suite) and not acceptable for any scored row — the copy went stale the moment the suite revised C1. Loop cells a suite wants scored are authored in the suite's own manifest by the suite, like any cell; the derived copy is retired, never re-synced.

Corollary for this repo: the harness implementation contains **no knowledge of any particular MCP server**. Any server-specific constant, tool name, cache path, fixture, or check found in the harness code is a defect against this contract. The implementation session should treat de-specializing the seeded code as conformance work against this file and `20-manifest.md`.

## Layer 1 — mechanical trace conformance (harness-evaluated)

Assertions a program can check against every trace record, with no judgment involved. The suite supplies them as declarative rules in the manifest (`checks`, ruling S2); the harness evaluates them and reports per-check results beside the run. What those rules assert is the suite's business — they encode the server's own tool contracts (the congressMCP suite asserted error-envelope structure, truncation markers, provenance completeness, and so on against its `40-tools.md`; see the example).

Exactly two Layer-1 behaviors are built into the harness rather than supplied by the suite, because they protect the instrument rather than the server contract:

- **Secret hygiene**: no configured secret material (anything the server registration marked as secret, plus values of env vars the suite names as sensitive) appears in any trace record, transcript, or meta artifact. This is not waivable by a suite.
- **Instrument liveness**: a run of a cell that produced zero trace records is reported as BROKEN, never as an abstention, a pass, or a vacuous result. A single-prompt cell with an empty trace is the canonical case. Liveness is also enforced **at spawn, fail-fast** (ruling S8): if the server under test exits, or fails to advertise its tools, before the consumer turn begins, the cell is BROKEN at that moment and the model turn is not spent — and the breach is surfaced to the operator as it happens, because an operator watching a run must be able to distinguish in-flight from broken. Grounded in the first real suite's first run (2026-08-31): a cwd-dependent launch command died at spawn with `server_exit` faithfully recorded in the proxy meta, yet three cells of model turns were spent against a dead server while the consumer answered from priors and the operator read the run as hung.

## Layer 2 — consumer-behavior findings (human/spec-scored)

What the pinned `pass`/`fail` criteria govern: did the consumer, given honest tool responses, produce an honest answer — completeness caveats propagated, absence reported as absence, no fabricated citations, provenance carried through. A failure is classified before it is filed:

- **Consumer-behavior finding** — the tool told the truth and the model dropped it; a response-shape or prominence question for the server's spec.
- **Tool defect** — the trace shows the server violating its own published contract.
- **Instrument defect** — the harness, driver, or cell configuration could not have captured the signal. Fix the instrument before any disposition; no finding of any other class is recorded from a defective instrument.

**Answerless turns are classified by cause, never lumped into harness failure (ruling S13, 2026-09-18).** A turn can end without an answer because the instrument broke (a harness failure: the cell is BROKEN) or because the consumer ran itself into a limit — an endpoint refused a request that the consumer's own tool calls had grown past the endpoint's context length, or the scaffold's step cap ended the loop. The second kind is a **consumer outcome**: the instrument captured everything, the trace is complete, and the suite's criteria score the row as a consumer that failed to answer. Artifacts carry a distinct outcome for it (name is the implementation's; `consumer_limit` with a cause of `context_length` or `step_cap` is the suggested shape) with `harness_failure` null, and the run report counts it beside pass and fail, never under broken. Grounded in `runs/2026-09-18-q8-data-policy/deny-r05`: the consumer paged three ~413KB windows into context, the endpoint refused the fourth request at 160,645 tokens against a 131,072 limit, and the row read as a harness failure although it is exactly the paging behavior the prompt's own fail clause names. The distinguishing rule is mechanical: a 4xx on a request whose recorded size exceeds the served endpoint's listed context length, after at least one tool call, is a consumer limit; any other upstream error stays an instrument outcome.

**A final message with no content is its own recorded outcome, never an answer (ruling S16, maintainer, 2026-09-21).** S13's family gains a member that is not a limit. Observed by the uscode-mcp spec session and confirmed here from the artifacts of `uscode-mcp/runs/2026-09-21T01:13:27-04:00-loop-complete` (harness 0.1.0, `openai/gpt-oss-120b` @ `deepinfra/bf16`, reasoning low, temperature 1.0): **7 of 26 rows** carry an `answer.txt` whose whole content is the four characters `null`, with `answer_chars: 4`, `harness_failure: null`, `consumer_limit: null`, and the one digest `74234e98afe7498f` — which is SHA-256 of the string `null`, so the harness serialized a JSON null and then hashed and counted it as an answer. In all four such rows that have a session file (`loop-floor` C3, C4, D1, D3) the final assistant message has `content: null`, no `tool_calls`, and a `reasoning_details` text that ends in a JSON object shaped like arguments to an offered tool (`{"query":"AUKUS","page_size":5}`), and the final wire line reads `finish_reason: stop`: the model wrote its next tool call into the reasoning channel, and the turn ended call-free. The three fresh-cell rows (`loop-isolation` A5, C4, D3) show the `null` answer and the `stop` and have no session file, so the reasoning-tail half of the signature is **unobservable** there — the reason the harness must record the classification itself. The contract: (i) the row's answer is the final message's content when that is a non-empty string, and nothing else is ever an answer — null or empty content yields an empty `answer.txt`, `answer_chars: 0`, no digest, and the row is not counted in `answered`; the text `null` is never written. (ii) The row records a distinct consumer outcome with `harness_failure` null — suggested shape: S13's block with cause `null_final_content` — counted beside the limits in the run manifest, never under broken and never as an answer. (iii) The loop consumer, which sees the whole response, records two derived marks with it: whether the final message's reasoning text ends in a parseable JSON object, and which offered tools' parameter names cover that object's keys; and the final `finish_reason` as already recorded. Marks are booleans and tool names and carry no text; the text itself belongs in the row's consumer transcript, which "Recorded per cell, per run" below already requires and which fresh loop rows in that run do not have (crowded rows have it as the `loop-session-*.json` file; the fresh rows' directories hold no message list at all) — a conformance gap, ordered closed in WO-7, and the reason three of the seven rows could not be fully read. (iv) Whether the fault is the model's or the serving stack's tool-call parser is not separable from here and is not claimed: it is a property of server × model at that endpoint, which is what a loop cell measures, and the suite scores the row as a consumer that failed to answer. (v) This is recording, not loop policy: `loop-scaffold@1` and its hash are untouched, and rows on either side of the fix are the same instrument. Rows recorded before the fix are identifiable after the fact by `answer_chars: 4` with digest `74234e98afe7498f`. Dead-defensive check on the harness's own record: zero of the 86 `answer.txt` files under `runs/` are a literal null, so no acceptance record here counted one. Ordered in WO-7.

**WO-7 accepted (2026-09-21, spec session, from `runs/2026-09-21-wo7/`, report `tests/WO7-acceptance.md` (at the commit cited; moved to `runs/acceptance/` by d4a6ebb, 2026-09-23) read second; commit 36102ab on `implement/wo-7`; 368 tests pass under the spec session's own run, 324 before; ruff clean; live spend $0).** Read from the artifacts, fresh and crowded runs alike, `--repeats 3` with the fake's repetition 2 ending on null content: that row's `answer.txt` is zero bytes, `answer_chars: 0`, `answer_sha256_16: null`, `harness_failure: null`, and `consumer_limit` carries `cause: "null_final_content"`, `reasoning_tail_json: true`, and `reasoning_tail_matches_tools` naming the offered tool whose parameters cover the object's keys (in the crowded run the distractor's same-shaped tool as well, which is the mark doing what it says and no more); `answers.A1` reads `invocations: 3, answered: 2, distinct: 1`; the run manifest's `consumer_limits` lists the row with its cause, zero harness failures, nothing voided; the progress output names the outcome as it happens. The pre-change run in `before/` reproduces the defect exactly — 4 bytes, digest `74234e98afe7498f`, `answered: 3, distinct: 2`. No retry: the affected row made the same number of wire requests as its neighbours (2 fresh, 4 crowded), `retries: 0`, and the scaffold hash in every artifact is the pinned `b92ad9f8…e2f6`. Every row now has a transcript of one shape, `{messages, offered_tools, scaffold}` — `loop-session-single.json` on fresh rows, `loop-session-<id>.json` on crowded — and a recursive search of each run directory finds the fixture's reasoning text in the r02 transcript and in no other file; the wire lines' key union is unchanged. The implementation kept `consumer_limit` as the one key for answerless consumer outcomes, as the work order allowed; accepted, with the name's misfit noted and not worth a rename. Product drivers: the report states neither path can reach the JSON serialization and that a successful empty output now records the same outcome; the first is a claim about code the spec session does not read, and the second is covered by hermetic tests only — no product row with an empty answer exists under `runs/` to read.

**One thing the implementation decided that the spec had not, ruled now.** The report says an observed call-free null final is exempt from the zero-trace liveness failure. The spec session did not order that, and liveness is one of the two built-in protections no suite can waive, so it does not get relaxed by implication. Ruling: the exemption stands **for loop rows only, and only on positive evidence in the row's own artifacts** — the spawn check passed, the wire surface equalled the manifest surface on every request, and the final response is on a wire line as a 2xx with a `finish_reason`. With those three the instrument is shown live and the consumer is shown to have been offered the tools and to have produced nothing, which is S16's outcome and not a dead instrument. A product-driver row with zero trace records and an empty answer has no such wire evidence of a completed final response and **stays BROKEN**. Reachability: a model that writes its *first* tool call into its reasoning produces exactly this row; none has been observed (all seven uscode-mcp rows had at least one trace record), and no artifact of the exempted case exists to read, so whether the implementation's exemption has these conditions is unverified. WO-8 asks for the artifact and for conformance if it differs.

**WO-8 accepted (2026-09-21, spec session, from `runs/2026-09-21-wo8/`; commit e2ae37b, merged to `main` by the maintainer; 383 tests pass and `ruff` is clean under the spec session's own run, 368 before; all three rows regenerated by the spec session with `tests/wo8_acceptance.py` (now `tests/scenarios/zero_trace.py`, d4a6ebb) into a scratch directory with the same outcomes; live spend $0 — the `spent_usd: 0.002` in the loop manifests is the fake upstream at `127.0.0.1` pricing its own usage).** The report `tests/WO8-acceptance.md` (at the commit cited; moved to `runs/acceptance/` by d4a6ebb, 2026-09-23) was read before the artifacts this time, not second; every statement below was then read from the row files, not taken from it. **The exemption did differ from the ruling, and the pre-fix artifact shows it:** `before-product/` is a product row with exit 0, a zero-byte answer, zero trace records, `harness_failure: null`, `failures: 0`, nothing voided, and `consumer_limits` listing it as `null_final_content` — a dead-looking product run counted as a consumer outcome, which is the case the ruling exists to prevent. After: the same row carries a harness failure naming the four requirements, the cell has a `CELL-VOID.json` with verdict BROKEN, and the manifest reads `failures: 2`, `consumer_limits: {}`, `zero_trace_cell_failures: ["basic"]`. The exempted loop row: one wire line (`http_status: 200`, `finish_reason: stop`, `tool_names` equal as a set to the three-tool manifest surface), `spawn-check.json` `ok: true` with the three tools advertised, a transcript whose only assistant message has `content: null`, no tool calls, and a reasoning text ending in a JSON object; `meta.json` has `trace_records: 0`, `harness_failure: null`, `consumer_limit.cause: null_final_content` with `reasoning_tail_json: true` and `reasoning_tail_matches_tools: [file_note]`, a zero-byte answer with null digest, `retries: 0`, the pinned scaffold hash, and a `zero_trace_liveness` block with `exempted: true`, the three observations, their denominators (`wire_requests: 1`, the expected surface), the spawn-check path, and the final response's `seq`/status/finish reason; the manifest reads `failures: 0`, one consumer outcome, nothing voided. The mismatch row: the fixture restricts the proxy to `read_note`, the wire line shows that one tool, `wire_surface_exact: false`, `exempted: false`, BROKEN. Denied rows carry the same block, so a scorer reads why either way (deliverable 2). The wire recorder gained `http_status` per line, a scalar; without it a 2xx could only have been inferred from the absence of an error note, which is the negative evidence the ruling refused.

**What these artifacts do not show, recorded so the acceptance is not read as more than it is.** (a) The mismatch row is broken by the S7 exact-surface breach that predates WO-8 — its `harness_failure` is S7's text and WO-7's exemption already excluded rows with a recorded breach — so it does not isolate the new surface condition. That is a weakness in the work order's choice of row, the spec session's, not in the implementation. The cases where the new conditions bind on their own (spawn evidence false or missing; wire trace missing or malformed; status missing or 500; `finish_reason` missing or empty; response incomplete; a product row handed an exemption-shaped record; a qualifying sibling beside a row with missing evidence) exist as ten parametrized hermetic tests plus two, all passing, read here by test id and result only — hermetic coverage, not artifacts. (b) "The row's own artifacts" in the ruling is satisfied for the spawn check by a **cell-level** file the row names by path (`spawn_check_path`); the spawn check is per cell by design (S8), so the ruling is to be read as *the row's run directory*, and the wording above is not otherwise changed. (c) A BROKEN row keeps its raw `consumer_limit.cause: null_final_content` for diagnosis while the run-level `consumer_limits` excludes it; on a product row that cause names something the harness did not observe (it saw empty output, not a final message), which is the misfit already noted at WO-7, now harmless only because the row is BROKEN. **A scorer must read `harness_failure` before `consumer_limit`**; `SUITE-AUTHORING.md` says so. (d) `failures` counts events, not rows: a one-row run reads `failures: 2` (the row's harness failure and the cell's zero-trace failure). (e) A zero-trace BROKEN cell has a `CELL-VOID.json` and an entry in `zero_trace_cell_failures` but **not** in `voided_cells` (the product run reads `voided_cells: {}`); the mismatch cell is in `voided_cells` only because of its S7 breach. Both (d) and (e) predate WO-8 and are recorded in `90-open-questions.md`, not ordered. Reachability is unchanged: no live row of the exempted shape has been observed.

**A crowding pre-turn that ends in a consumer outcome: the row is unscored, and says why (ruling S17, spec session, 2026-09-21).** Observed by the maintainer in a live run and read here from `uscode-mcp/runs/20263721183723-loop-complete/loop-floor/A/A2/r01/` (loop-consumer/0.1.0, `neutral-file-triage@2`, DeepInfra ×4, run still in progress when read; 1 of the 5 rows present, the other 4 answered). The pre-turn listed the notes, read `n01`, filed `n01`, and then ended on S16's signature exactly: four wire lines, all 200, finish reasons `tool_calls`, `tool_calls`, `tool_calls`, `stop`; the last assistant message has `content: null`, no tool calls, and a reasoning text ending `Next n02.{"note_id":"n02"}`; `loop-result-crowding.json` records `consumer_limit.cause: null_final_content` with `reasoning_tail_matches_tools` naming `file_note` and `read_note`; `distractor-state.json` holds `n01` alone, where the pinned procedure's mid-task state is `n01`–`n04` (`40-instruments.md`). The harness did not send the scored prompt and recorded `harness_failure: "crowding pre-turn exited 4; …"`, `exit_status: -1`, `consumer_limit: null`, `zero_trace_liveness: null`, and the pre-turn's spend. **What is right and stays:** the scored prompt is not sent. A consumer one note into a four-note pre-turn is not the pinned state, its context is a different size from its neighbours', and a row scored from it would not be the cell's instrument; and no retry inside the pre-turn, which runs under the same hashed scaffold (S16 corollary). **What is wrong, and is ruled:** (i) the row's `meta.json` does not say why. The cause is in two side files and nowhere in the row's own record, and the message names an exit code. The row records the pre-turn's outcome block — cause and the two reasoning marks, as the consumer reported them — beside `loop.preturn`, and the failure text names the cause. (ii) It is counted as an instrument failure with nothing to tell it from one. It is not a broken harness: the recorder, the spawn check, the surface and the endpoint all worked, and the consumer failed to get through the setup task — S16 (iv)'s server-stack-or-model fault, arriving one turn early. It is also not a consumer outcome of the scored prompt, which was never asked. It is a third thing: **precondition unmet — no measurement taken**. The row stays out of `answered`, out of `consumer_limits`, and unscored; the run manifest counts such rows under their own key with the cause, and they do not add to `failures`, so that `failures` keeps meaning *the instrument could not be trusted* and an operator can tell endpoint behaviour from a harness defect at a glance. Cell liveness is untouched: a cell whose every row ended this way has zero trace records and is BROKEN as before. (iii) **Dropping these rows does not bias the rows that remain**, and this is by construction, not by measurement: the scored prompt is not in the pre-turn's context (the transcript holds the system prompt and the procedure's opening prompt only), so whether the pre-turn completes cannot depend on which prompt would have followed. The surviving rows are a sample conditioned on *the consumer reached the pinned mid-task state*, which is the floor cell's definition. What it does cost is N: a suite asking for ten repeats gets fewer, and the rate is not small — this endpoint ended 7 of 26 scored turns this way, and a pre-turn is four or more requests. Whether the harness makes up the shortfall is a requirements question, put to the maintainer as Q11. (iv) **Operator legibility (S9):** the progress line for this row was cut at the terminal's width before it reached either the cause or the path — `HARNESS FAILURE: crowding pre-turn exited 4; the consumer is NOT mid-task, so running the scored prompt wou`. A failure line leads with the cause in its first words and the full text is printed untruncated, on its own line if need be; a line that loses the reason to truncation fails S9's test, since the operator had to guess (correctly, as it happened) and ask. Ordered in WO-9.

**A row lost to an unmet precondition is replaced, up to three times (ruling S18, maintainer, 2026-09-21; answers Q11).** The maintainer's words: retry up to 3 times; the model is cheap and the extra cost is accepted. Contract: when an invocation ends *precondition unmet* (S17), the harness runs that (cell, prompt, repetition) slot again as a **whole fresh invocation** — new neutral working directory, new server process, `setup` and the pre-turn re-run, exactly as S15 defines a repetition — until one attempt reaches the scored turn or **three replacements** have been made, four attempts in all. Three is the default and a run parameter like `--repeats` (suggested `--precondition-retries N`, 0 turns it off); like N it is a property of the run and never of the manifest, so the manifest hash and `cell_id` are untouched. **Scope, stated because the word *retry* is loaded here: this replaces invocations whose scored prompt was never sent, and nothing else.** A scored turn that ends in a null final, a step cap, or a context limit is a measurement and is kept as the slot's result, never replaced — S16 and its corollary stand unchanged — and a harness failure of any other kind is not replaced either, since re-running a broken instrument hides the break. Nothing is re-prompted and `loop-scaffold@1` is untouched; S17 (iii) is why replacement cannot select on the scored prompt. Accounting: every attempt is kept on disk and in the manifest, none overwritten; the slot's row is the attempt that reached the scored turn, and the unmet attempts stay counted under S17's key, each naming its slot and attempt number; a slot that exhausts its attempts is recorded as such, unscored, and the run continues. The per-(cell, prompt) counts read asked-for, reached, and attempts made, so the unmet rate per endpoint is readable from any run (PM-2's denominator). Spend: the pre-run estimate states the worst case (every slot using all four attempts) beside the expected figure, replacements draw on the same cell and run budgets, and a budget stop ends replacement like anything else. Operator legibility: each replacement is announced as it starts, with the cause that prompted it. Ordered as WO-9 part B.

**WO-9 accepted, both parts (2026-09-21, spec session, from `runs/2026-09-21-wo9/`, eleven runs, artifacts read before the report `tests/WO9-acceptance.md` (at the commit cited; moved to `runs/acceptance/` by d4a6ebb, 2026-09-23); commit c289b74 on `implement/wo-9`; 407 tests pass and `ruff` is clean under the spec session's own run, 383 before; `replaced`, `exhausted`, and `scored-null` regenerated into a scratch directory with the same counts and slot states; live spend $0, the dollar figures being the fake upstream's).** Read from the rows: `before/` reproduces the live defect on e2ae37b (exit-code message, `failures: 1`, the terminal line cut at 90 characters); `after/` on the same fixture and manifest hash has r01's `harness_failure` beginning `null_final_content: crowding precondition unmet: …; scored turn not sent`, a `precondition_unmet` block with `stage: crowding_preturn`, the cause, the detail, and both reasoning marks, two wire lines (one tool call, then `stop`) and no scored request, `failures: 0`, the manifest's `preconditions_unmet` naming the slot and attempt, `invocation_counts.loop-cell.A1` reading `asked 2, reached 1, attempts 2`, and the full failure text on its own terminal line. `all-unmet/`: both attempts unmet, `CELL-VOID.json` BROKEN for zero trace records, `failures: 1` being the cell. `crash/`: a pre-turn exiting 2 with no consumer outcome stays an ordinary harness failure, slot `failed`, not replaced. Part B: `replaced/` — r01 attempt 1 unmet at state `{n01}`, attempt 2 in `r01/attempt-02/` with its own neutral cwd, server config, transcript, wire, and `distractor-state.json` reading `n01`–`n04`; `slots` selects the second, `asked 2, reached 2, attempts 3`; the terminal announces `REPLACEMENT: null_final_content: loop-cell/A/A1/r01 attempt 2/4, whole fresh invocation`. `exhausted/`: four attempts, all kept, slot `exhausted`, r02 scored, run continued, `attempts 5`. `scored-null/`: two scored turns ending on null content, both kept on attempt 1 under `consumer_limits`, nothing replaced (S16 stands). `budget/`: a 0.004 cap, two attempts, stop recorded under `budget.stops` with the cap and the spend, slot `budget_stopped`, r02 `not_started`, cell BROKEN for zero trace. `disabled/` (`--precondition-retries 0`) equals `after/`; `clean/` has no attempt directories, so a run without an unmet invocation has yesterday's layout. `selection.precondition_retries` is in the manifest record and outside the hash, the pre-run block reads expected 0.019974 and worst case 0.079896 (four attempts per slot) and the terminal prints both. **Three small things read from the artifacts, ordered as WO-10 part B, none blocking:** (i) `answers.A1.invocations` in `cell_marks`/`loop_cells` reads 3 in `replaced/`, counting the unmet attempt that never received the prompt, while `reached` reads 2 — the two must agree or `answered / invocations` misreads; (ii) the budget-stop line prints a 0.004 cap as `$0.00` and says `cell loop-cell: not started` after two attempts had run; (iii) `preconditions_unmet` keys are relative paths and `slots` keys are slots, which is fine, but the report's word "unmet" and the manifest's `exhausted` status on a slot with retries disabled (one attempt, nothing to exhaust) read oddly; a status name that says *no replacement allowed* would be clearer.

**The product-driver report back, and what it exposed (ruling S19, spec session, 2026-09-21).** `product-empty/`: a product-family pre-turn that exits 0 and writes nothing is treated as a completed pre-turn — both scored turns ran and answered, `reached 2`, no `distractor-state.json` in the row at all. `product-crash/`: exit 2 is a harness failure, as it should be. So on a product driver the harness cannot tell a pre-turn that did the task from one that did nothing, and on the loop driver it tells them apart only because `loop_consumer` exits 4 on a consumer outcome since WO-7 — **the precondition is inferred from an exit code, never checked.** The state that defines the precondition is on disk in every crowded row (`distractor-state.json`), the procedure that defines it is harness-owned and pinned (`40-instruments.md`), and the check is one set comparison. That this matters is not hypothetical: a scan of every live crowded row in both repositories (2026-09-21; 87 rows of `neutral-file-triage@2`; the fake-upstream acceptance runs excluded) finds **three rows that reached the scored turn with the wrong state** — `uscode-mcp/runs/2026-09-21T01:13:27-04:00-loop-complete/loop-floor` A3 at `{n01}`, A2 and C2 at `{n01, n02}`, all three from before WO-7, when a pre-turn ending on null content wrote the text `null`, exited 0, and the scored prompt went to a consumer one or two notes into a four-note task. Those three rows are **not mid-task under S6 and are instrument-defective for the crowded condition**; their disposition is the uscode-mcp spec session's (they own the scoring), handed over below. The other 76 reached rows are at exactly `n01`–`n04`, and the 8 unmet or failed rows are at 1–3, so the predicate separates the two populations cleanly on the record. Ruling: **a crowding procedure pins its mid-task predicate, and the harness checks it from the recorded state after every pre-turn, on every driver, before the scored prompt is sent.** A pre-turn whose state fails the predicate is *precondition unmet* (S17) with its own cause — suggested `state_mismatch`, carrying the expected and observed sets — whatever the consumer's exit status or output, and it is replaced under S18. The consumer-outcome exit remains a cause where the consumer reports one; the state check is what makes the label independent of any driver's ability to report. This also closes the original v1 defect mechanically: a pre-turn that runs the triage to completion fails the same predicate from the other side (`40-instruments.md` → v1). For `neutral-file-triage@2` the predicate is: the filed set is exactly `{n01, n02, n03, n04}`, folders unconstrained; pinned in `40-instruments.md`. Rows recorded before the check are identifiable after the fact by their `distractor-state.json`, and a suite's scorer can apply the predicate by hand until the harness does. Ordered in WO-10 part A.

**WO-10 accepted (2026-09-22, spec session, from `runs/2026-09-22-wo10/`, ten runs, artifacts read before the report `tests/WO10-acceptance.md` (at the commit cited; moved to `runs/acceptance/` by d4a6ebb, 2026-09-23); commit f4d4cb3 on `implement/wo-10-wo-11`; 432 tests pass and `ruff` is clean under the spec session's own run after WO-11 too; live spend $0).** Read from the rows: `two/` and `five/` — a pre-turn that filed `{n01, n02}` and one that filed `{n01..n05}` (v1's post-task shape) both end `state_mismatch` with `expected` and `observed` sets in the row's `precondition_unmet` block, the failure text leading with the cause, the slot replaced and scored on attempt 2, `asked 2, reached 2, attempts 3`; `replaced/` — a consumer-reported null final keeps its cause `null_final_content` and gains the state evidence (`observed: [n01]`); `product-empty/` — a product-family pre-turn that exits 0 and writes nothing is now `state_mismatch` at `observed: []`, replaced to exhaustion on both slots (8 attempts, `asked 2, reached 0`), cell BROKEN for zero trace; `product-pass/` — a product pre-turn that reaches `n01`–`n04` runs its scored turn; `missing/` and `unreadable/` (state file absent; state file `{broken`) — harness failures naming the path and the error, not replaced, cell BROKEN; `clean/` — no attempt directories, and every reached row now records `crowding.state_check: {expected, observed, passed: true}`, so a scored crowded row states its precondition rather than implying it. **Where the predicate lives:** outside the procedure's content hash — v1 and v2 hashes unchanged, `3197f43e…` in every row — on the implementation's stated reasoning that the predicate is instrument validation and not consumer-visible content. Accepted: the consumer never sees the predicate, so it is not S6 content; it is pinned by text in `40-instruments.md`, and a change to it is a spec commit there, which is a weaker pin than a hash and is recorded as such.

**WO-11 accepted (2026-09-22, spec session, from `runs/2026-09-22-wo11/`, artifacts read before the report `tests/WO11-acceptance.md` (at the commit cited; moved to `runs/acceptance/` by d4a6ebb, 2026-09-23); commit b93c9b0; DR-4 closed).** The regression matrix, each run carrying a check that fails: `flagless-replaced/` — the shape that killed the maintainer's run — produces a checks report whose failure reference reads `cell`, `prompt_id: A1`, `repetition: null`, `attempt: 2`, `index: 0`, `invocation: loop-cell/A/A1/attempt-02`; `repeated-replaced/` reads `repetition: 1, attempt: 2` and `repetition: 2, attempt: 1`; `flagless-clean/` `repetition: null, attempt: 1`. `checks-exception/` (an exception injected into the evaluator): the run manifest carries `reporting.checks: {status: error, exception: …}`, a checks report exists with the check marked `error` and the exception named, the terminal prints `REPORTING ERROR: checks: … missing completed checks-report.json`, and the run counts one harness failure — the reporting stage, named, and no row; `measurements-exception/` the same for that stage with the checks report complete. **The maintainer's run, rebuilt:** `mcp-e2e report --run-dir` on `uscode-mcp/runs/20260922T120550Z-e22-secondary` wrote `run-manifest.json` and `checks-report.json` into that directory with `rebuilt: {at, harness_version, reason}`, `pre_run: null`, budget fields null with the recorded row spend beside them ($0.02688), `asked: null` where only the runner knew, 22 rows, 4 unmet attempts, both replaced slots `scored` (C3 at attempt 4, C2 at attempt 2), and `evidence-verification.json` recording 345 pre-existing files SHA-256-unchanged. Checks over the rebuilt run: 11 pass, 4 vacuous, 1 fail — and the fail is `banner-leads-truncated-content`, failing in exactly the two `-field` arms (`e22-floor-field` 4 matched, `e22-isolation-field` 3 matched) and passing in every other cell: the by-design arm signature the uscode-mcp session preregistered, legible per cell on a live run for the first time, which is what the 2026-09-21 granularity ruling was for. Path-parsing sites: the report lists them; the spec session does not read the code and takes the matrix as the evidence.

**An upstream refusal that exhausts the consumer's retries is an instrument outcome with a cause of its own (ruled 2026-09-23, spec session; relayed by the maintainer from the uscode-mcp spec session).** Read from `uscode-mcp/runs/20260923T021835Z-e25` (4 arms × 3 prompts × `--repeats 10`, $0.083): five rows ended when the endpoint answered HTTP 429 three times in a row (two retries, 2 s and 5 s, then the loop gave up). Each is recorded as `harness_failure: "runner exited 2: <stderr tail>"`, `consumer_limit: null`, `answer_chars: 0`, `loop.retries: null`, with the cause readable only inside the stderr text and `loop.consumer_error`, and the slot's status reading `scored`. S13's rule already places this on the instrument side — any upstream error other than a context limit stays an instrument outcome, and it is right that it does: nothing about the consumer is measured by a provider being busy. What is wrong is the same defect S17 (i) fixed for pre-turns: the failure names an exit code, not a cause, so an empty answer with no consumer outcome has to be told from a null final by reading a traceback. Contract: (i) the failure text leads with a cause, `upstream_unavailable`, and the row carries a block with the HTTP status, the provider named in the response, the attempts made and the retry schedule, the step reached, and whether any tool call had been made; `loop.retries` is recorded on a failed row exactly as on a completed one. (ii) The slot's status reflects the row's disposition: a row that is a harness failure makes its slot `failed`, whichever turn failed; `scored` means a scoreable row exists. (iii) Rows are attempted, never answered, and count under `failures`, as now. **Replaced (ruling S20, maintainer, 2026-09-23; answers Q12):** a slot whose invocation ends `upstream_unavailable` is replaced exactly as an unmet precondition is under S18 — a whole fresh invocation, the same attempt counter (`--precondition-retries`, default 3), the same budgets, every attempt kept and counted, the cause in the attempt record, the replacement announced with its cause. The never-replace rule for every other harness failure is unchanged; what qualifies is a refusal the loop retried and the endpoint kept refusing (429, 5xx, timeouts after retries), never a breach, a spawn failure, or a recorder error. The unmet-attempt rate and the unavailable-attempt rate are counted apart in the per-slot record so PM-2's denominator stays a consumer figure. Ordered in WO-12 (part A the cause, part B the replacement).

**WO-12 accepted, both parts (2026-09-23, spec session, from `runs/2026-09-23-wo12/`, five runs, artifacts read before the report `tests/WO12-acceptance.md` (at the commit cited; moved to `runs/acceptance/` by d4a6ebb, 2026-09-23); commit 01cdd33 on `implement/wo-12`; 453 tests pass and `ruff` is clean under the spec session's own run; live spend $0).** Read from the rows: `part-a/` (replacement off) — the refused row's `harness_failure` leads `upstream_unavailable: provider=FakeProvider status=429 after 3 attempts`, the row carries `upstream_unavailable: {http_status: 429, provider, attempts: 3, retry_schedule_s: [2.0, 5.0], step: 2, tool_calls_before: 1}` and `loop.retries: 2`, the slot reads `failed`, `failures: 1`, `answers` `invocations 2, answered 1`. `replaced/` — the same refusal on attempt 1, attempt 2 in `attempt-02/` scored, slot `scored` with `attempts 2`, counts `attempts_unavailable: 1, attempts_unmet: 0`, `failures: 0`, the terminal's `REPLACEMENT: upstream_unavailable: … attempt 2/4, whole fresh invocation`; `server-error/` the same on a 503. `exhausted/` — four refused attempts on each of two slots, every attempt kept, both slots `exhausted`, the cell counted once under `failures`. `budget/` — a cap reached after the refused attempt: slot `budget_stopped`, the next `not_started`, nothing replaced past the cap. **Rebuilds verified by the spec session's own hand:** `report --run-dir` on copies of `part-a`, `exhausted`, and `replaced` with the run-level files removed reproduced every slot status and attempt count and every invocation count, differing only by the rebuild's `observed_slots` key. **One reading pinned so the counts are not misread:** `reached` counts attempts whose scored turn was *sent*, including ones the endpoint then refused (`exhausted/` reads `reached 8, attempts_unavailable 8`), so the consumer denominator for a rate such as PM-2's is `reached − attempts_unavailable`, and `answers.invocations` likewise includes refused attempts while `answered` does not. Recorded here rather than reordered: the two numbers a scorer needs are both on the record.

## Run mechanics

Live consumer per run; no replay tier. Runs are minimal and manual: high-risk changes and pre-release, at the suite owner's discretion.

**Repeats are a run parameter (ruling S15, maintainer, 2026-09-21).** `mcp-e2e run --repeats N` invokes every selected (cell, prompt) pair N times in one run. N belongs to the run and never to the manifest: the manifest hash is untouched, so rows from a repeated run, from N separate runs, and from different arms stay comparable on the usual rule. Binding details, the spec session's: (i) **every repetition is a whole invocation** — fresh neutral working directory, fresh server process, `setup` re-run, crowding pre-turn re-run (S5 admits no warm-by-accident, and a repetition that inherited the last one's server would be exactly that); the loop calibration probe stays once per cell per run, as now. (ii) **Repetition is the outer loop:** the runner makes pass 1 over the whole selected grid, then pass 2, and so on, so that arms compared within one run are interleaved in time — a provider's bad quarter-hour lands on every arm and not on one — and a budget stop leaves the arms with counts that differ by at most one. (iii) **Layout:** when the flag is given, including `--repeats 1`, a row lives at `<cell>/<group>/<prompt id>/rNN/` (two digits, 1-based, wider only if N needs it); when it is absent the layout is what it always was, so no existing reader or cited path breaks. Readers key off the artifacts and not the path: every row's `meta.json` carries `repetition` (1-based; null without the flag) and the run manifest's `selection` carries `repeats` (null without the flag). (iv) **Budget and estimate:** the pre-run estimate and stated invocation count multiply by N; cap semantics are unchanged, and a cell-cap stop skips that cell's remaining prompts and repetitions. (v) **The answers count becomes real:** per prompt within a cell, `answers` records `invocations` (rows attempted), `answered` (rows with a non-empty answer), and `distinct` with `digests` over the answered rows only — an answerless row (a consumer limit, a harness failure) has an empty answer, and N empty answers hashing alike would read as N replays of one draw; that case is reachable (`runs/2026-09-18-q8-data-policy/deny-r05`). (vi) Layer-1 checks evaluate over all repetitions' records, and a failure reference gains the repetition (`30-checks.md`). Nothing here makes a repeat a sample — `50-drivers.md` → loop, requirement 11 stands, and the flag is what finally lets one run manifest show the distinct-answer count that requirement asks for. Preregistered with the order (WO-6, part B): expected — under the fake upstream, three repetitions of one pair where the fake returns two different answers record `invocations: 3, answered: 3, distinct: 2`, three server spawns, and `setup` recorded in each repetition's meta; falsifier — any repetition whose artifacts show a reused server process or a missing setup record, or a count that disagrees with the three `answer_sha256_16` values.

**WO-6 part B accepted (2026-09-21, spec session, from `runs/2026-09-21-wo6/`, report read second; commit 5d0364a).** Preregistered outcome met, no falsifier observed. Read from the artifacts of the `--repeats 3` run: six rows at `<cell>/A/A1/r01..r03`, each `meta.json` with its `repetition`; six distinct server spawn pids in `proxy-meta.json` → `server_spawn` and six distinct working directories; a `setup` record with its response in every repetition; `started_utc` ordering control r01, treatment r01, control r02, treatment r02, and so on — the outer loop as ruled; `answers.A1` reading `invocations: 3, answered: 3, distinct: 2` for each cell, agreeing with the three `answer_sha256_16` values, each of which the spec session recomputed from `answer.txt`; `selection.repeats: 3`; the pre-run output stating 6 invocations and a per-cell estimate for 3. The answerless run reads `invocations: 3, answered: 2, distinct: 1` for the affected cell, the row a `consumer_limit` with `harness_failure` null. `--repeats 0` is refused as a usage error. **The flagless comparison, and a defect in the work order that the implementation reported and did not paper over:** paths identical (33 files before and after), no key removed; keys added are `repetition` and `selection.repeats` (null), and also `env` in row meta, `answered` in every `answers` block, and the per-cell and row-naming fields of the checks report. WO-6's text said a flagless run would differ by the two null fields only, which contradicted its own part A and its own item 12 — the spec session's drafting error. Ruling: the contract for a flagless run is **identical paths, no key removed or changed in meaning, additions only as ordered**; the work order is corrected to say so. One observation outside WO-6's scope, recorded here so it is not lost: the answerless replay was classified a consumer limit from the endpoint's own stated numbers although the request (~379 tokens) was far under the listed context length (131,072); S13's operational form in `50-drivers.md` gives the listing precedence, so which source wins when both exist and disagree is worth a look the first time a live row raises it — the fake was built to produce the shape, and no live row has.

**Reporting never loses a completed run (ruled 2026-09-22, spec session; DR-4).** The rows are the measurement; the run-level artifacts (`run-manifest.json`, `checks-report.json`) are derived from them plus what only the runner knew (selection, estimate, budget events, slot states, driver versions). Grounded in `uscode-mcp/runs/20260922T120550Z-e22-secondary`: every invocation completed and a reporting-stage exception left no run-level artifact at all. Contract: (i) **the run manifest lands before any derived stage runs** and is rewritten as each stage completes, so that a crash in checks or measurements leaves a manifest that names the stage that failed and the exception; the exit status is non-zero and the terminal says which artifact is missing and why. (ii) **A stage that raises is recorded as that stage's error outcome, never as absence** — the four-outcome rule applied to the evaluator itself: a checks evaluation that cannot run writes a checks report in which every check is `error` with the exception named, exactly as a single bad regex would. (iii) **`mcp-e2e report --run-dir DIR` (name is the implementation's) rebuilds the run-level artifacts from the rows on disk**, for a run that crashed in reporting or was killed mid-run. A rebuilt manifest says it was rebuilt, with the harness version that rebuilt it, and lists what it could not recover — the pre-run estimate and any budget or replacement events not recorded per row — as null with a note, never as zeros. Rows already carry `repetition`, `attempt`, and `precondition_unmet`, so slot states are recoverable from rows alone; if any run-level fact is not, the fix is to record it in the row, not to declare it unrecoverable. This is what makes the maintainer's run scoreable without re-spending it. Ordered in WO-11.

**Attribution — the two-source rule.** The cell's environment must make answers attributable to exactly two sources: the model's priors and the server under test. Binding consequences:

- Web/network and other non-server tools disabled, and their absence **verified by observation, not asserted from configuration** (ruling S7). A driver flag that requests disablement is a claim; it was measured insufficient on 2026-08-31, when a driver's deny-flag blocked invocation but left every builtin on the consumer's surface. The verification tiers: where the driver's model-API traffic can be transparently interposed, the harness records the actual tool surface sent to the model on **every scored invocation** — wire-level ground truth, invisible to the consumer; where it cannot, a discarded same-configuration calibration probe gates merge-gating cells, valid only per recorded driver version. A disallowed tool observed on the wire, or a capture that saw zero model requests, is an instrument breach — never a clean row.
- No persistent memory, and a neutral working directory outside both the harness repo and the suite repo, so no CLAUDE.md, spec file, or repo context leaks into the consumer. A consumer that learns it is being tested, or what the internals look like, is a different consumer (the no-disclosure principle).
- A driver that cannot verifiably satisfy this (e.g. cannot disable its own web fetching) is refused for attribution-dependent cells; the harness fails the cell as instrument-broken rather than running it unattributable.

**The wire recorder's recording contract.** The recorder that provides S7's API-boundary capture is strictly transparent — it never originates, modifies, or reorders traffic; it only observes. What it records per request: path, model, tool names with their `tool_source`, body keys, and (added 2026-09-02, for context-accounting measurements such as uscde-mcp's E15) the serialized body size in bytes (decompressed where parseable; wire bytes with the encoding named otherwise). It records **no** headers, no message content, no tool schemas. Response bodies are recorded only via an explicit allowlist of named scalar extractions, which currently has exactly one entry: the numeric token count returned by a count-tokens endpoint (the endpoint of whichever API shape the driver speaks; one entry, because it is one scalar for one purpose), when the driver itself made that call — recorded on that request's own line. Extraction decodes whatever response encoding the driver negotiated (the recorder cannot narrow the negotiation without modifying traffic); an encoding it cannot decode is recorded as a null scalar with the encoding named, and counted in the per-cell digest as a call whose scalar went unread — a failed read is never recorded as an absent call (DR-3). **Entries added 2026-09-18 for the loop driver** (`50-drivers.md` → loop, requirements 4 and 6; measurement that needed them: `openrouter-probe-2026-09-18.md`, served-provider variation and per-request cost), all from the OpenAI-compatible chat-completions response: `provider` (the served-provider string), `usage.prompt_tokens`, `usage.completion_tokens`, `usage.completion_tokens_details.reasoning_tokens`, and `usage.cost` — five scalars, recorded on the request's own line, null with a note when absent or unreadable. **Sixth entry, added 2026-09-18:** `choices[0].finish_reason` (measurement that needed it: three pinned loop C1 completions sat at the 4,096-token `max_tokens` cap with ~20k-character answers cut mid-paste, and no artifact could tell a cap-truncated answer from one the model ended itself; a scorer reading claimed-versus-delivered must be able to). The loop consumer itself reads the full response, as any consumer does; the allowlist governs what the recorder writes into artifacts, never what the consumer sees. **Timing (added 2026-09-18, WO-1 finding 5):** each line records `at` as request arrival and `duration_ms` from arrival to the end of the response; a reader timing a slow backend from `at` alone was misled by a 356-second completion whose two lines sat 4 seconds apart. Because a line is complete only when its response is, lines land on disk in completion order; `seq` is assigned at arrival and is the order to read by. Growing the allowlist is a spec commit here, never an implementation convenience — each entry names the endpoint, the scalar, and the measurement that needed it. Per S10, this recorder is the only admissible source for claims about what reached the model; sizes and counts are scalars, so nothing new becomes publishable from artifacts.

**Knobs are recorded in the driver's native vocabulary, verbatim, never translated across vendors.** A Claude cell carries `thinking`; another vendor's cell carries that vendor's terms; no mapping between vendors' scales is recorded or implied, because none is defensible.

**Crowding is part of the instrument** (ruling S6). The `crowded` context condition is produced by a harness-owned, versioned crowding procedure: a coherent, mundane, domain-neutral task the consumer is genuinely mid-way through when the prompt lands, with other tools registered. Suites select a procedure by name; they never author crowding content, because the crowding sits in the consumer's context during the scored turn and a suite's spec session — which knows the internals — would be writing part of the instrument it is scored against (the same disqualification-by-construction that bars it from authoring real-user prompts). Trivia padding is rejected as the default on the design assumption that padding yields tokens without a competing goal and therefore under-crowds; that assumption is empirical and its settling measurement is recorded in `90-open-questions.md` Q5, not asserted here. No fixed corpus is inert for every server, so suite authorship includes a **domain-collision attestation** for the selected procedure; on collision the suite selects a different harness procedure.

**Setup actions, never model turns.** A cell may declare setup actions (see `20-manifest.md`): direct calls the harness makes against the server before the prompt — pre-warming a cache, seeding state. These are performed server-side by the harness, never by a model turn, and exactly what was done is recorded in the cell's meta artifact.

**Recorded per cell, per run:** manifest content hash; cell id and the full knob set; the complete tool-call trace; the consumer transcript; the recorded available-tool surface; setup actions performed; for crowded cells, the crowding procedure's name, version, and content hash; and a meta record (wall clock, tool-call count, and the upstream identifiers actually hit, so staleness of groundings is detectable after the fact rather than assumed away); and, when the suite declares them, row measurements (`30-checks.md` → "Row measurements"), recorded beside the row with their method name and hash, never as outcomes. The minimum trace-record fields are pinned in `30-checks.md`, since checks evaluate against them.

**Operator legibility** (ruling S9). The runner is never silent: it reports progress to its controlling terminal as it works — which cell and prompt it is on, turn transitions within a cell (setup, pre-turn, scored turn), and per-cell completion with at least wall clock and trace-record count — and it names the run directory up front, so an operator who suspects trouble knows where the artifacts are before anything has gone wrong. Format is the implementation's; the requirement is that the three states an operator must distinguish — in-flight, hung, broken — are distinguishable from the terminal without digging. Grounded in the DR-1 incident: the runner emitted nothing, so a progressing run was read as hung, and a run that was in fact broken (dead SUT, priors-only answers) was diagnosed only when the operator escalated — the artifacts held everything needed, but nothing pointed at them.

Run bytes land in a gitignored `runs/` directory — bytes are disposable; the scored findings are what gets committed, in the suite repo.

## Interview mode — an unscored follow-up on a scored loop row (ruling S21, 2026-09-23)

**What it is.** After a loop row's scored turn has run to a recorded end, the harness can resume that row's recorded session and put one or more operator-written questions to the same model in the same context — the scaffold, pin, and knobs of the row, the transcript it produced, the tool surface mounted so the model may call tools while answering — and record the exchange beside the row. Requested by the uscode-mcp suite (its record dated 2026-09-24; maintainer-approved there the same day), whose trace-only rows kept needing one question no artifact can answer: why a consumer stopped after a refused call, what a caveat told it to do, why it presented one of two listed rules as unique. Two of that suite's findings were this instrument run by hand on capable consumers and measured real afterwards; the loop driver can run it on the floor. Ordered as WO-13.

**What it is not, and this binds.** An interview is self-report, and S10 governs it entirely: its answers are never evidence for a finding of any class, never a check outcome, never a row measurement, never a Layer-1 extension. They enter a suite's spec only as claims to preregister a measurement against. It is not scored and never pools with rows. Where it pays is choosing the next measurement — a failed row in any cell, and the more capable lineage whose self-reports are worth more; least on the floor model's account of how a tool felt.

**Binding mechanics, the spec session's:** (i) **The operator's question is the only new content.** It is sent verbatim as a user message; the harness ships no preamble, framing, or template, so that what the model was asked is exactly what the record says it was asked. Questions are recorded verbatim, in order, with their answers, so a quote is traceable to its question. (ii) **Same context means the recorded transcript replayed** under the row's scaffold hash, pin, knobs, and `require_parameters`, with the MCP server spawned fresh (S5); the interview records the digest of the session file it replayed, whether recorded reasoning content was sent back to the endpoint, and the served provider per request. Wire capture interposes and the S7 surface comparison runs, recorded in the interview's own meta. (iii) **Eligible rows:** loop rows whose scored turn ran to a recorded end — `harness_failure` null, whatever the consumer outcome. A harness-failed row, a precondition-unmet or upstream-unavailable attempt, and a product-driver row are refused with the reason. (iv) **Writes nothing into the scored artifacts.** The exchange lands under `interview/NN/` in the row directory with its own wire lines, session file, trace, and meta marked `interview: true, aggregate: false`; the run manifest, checks report, measurements, liveness, answer counts, slot states, and `report --run-dir` rebuilds are byte-identical with or without it, and the row's own files are untouched. (v) **Cost and provider are recorded as for any turn**, the pre-request estimate names its basis (every question re-sends the whole prefix), and `--budget-usd` applies to the interview alone. (vi) Not in the manifest hash, not in cell identity, not a run parameter: a row with an interview is the same row.

**WO-13 part A accepted (2026-09-24, spec session, artifacts read first, report second; implementation commit 7a26eb2, nothing under `documentation/`; the interview test module re-run by the spec session, 23 passed).** Live interview, one question, `uscode-mcp/runs/20260923T021835Z-e25/e25-akashml-notemp/C/C4/r10/interview/02/`: the replayed prefix equals the row's `loop-session-single.json` messages (11 of 11, byte-identical, offered tools and scaffold hash `b92ad9f8…e2f6` identical); the two new messages are the user text `Why did you stop?` exactly as given and the assistant's answer; one wire line, four-tool surface equal to the expected surface, `verified: true`, served provider AkashML against pin `akashml/bf16`, `finish_reason: stop`, 10,246 prompt tokens against the row's final 10,132 (the prefix went back, plus the question), spend $0.00032914 against a $0.05 budget, `reasoning_replayed: true` with the recorded `reasoning` field sent verbatim and accepted; a fresh server pid (81446 against the row's 90188); the row's own files untouched (mtimes 2026-09-22, the implementation's digest check agreeing). `interview/01/` on the same row is a harness-failed interview — the server died at spawn for a missing secret, the question marked `asked: false` with the reason, $0 spent, nothing sent — and it was not overwritten by the retry, which is the numbering rule working. Fake-upstream run (`runs/2026-09-23-wo13/acceptance/…/run/`, a manifest declaring one check and one `answer-coverage@1` measurement): a two-question `--ask-file` interview on `loop-cell/A/A1/r01` whose first question is `"  Why did you stop?\nDo explain. 🦉"` — leading spaces, newline, and emoji preserved in `questions.json` and in the transcript — each question answered through one tool-call round (4 wire lines, 2 trace records, body sizes 2,622 → 3,510 → 3,627 → 4,515 bytes, so the second question carried the first exchange), a second interview `02/` beside it, both prefixes identical to the row's session; the row's session file now carries `tool_definitions`. **Rebuild, re-run by the spec session:** `report --run-dir --overwrite` on a copy with the interview trees and on a copy without them gives byte-identical `checks-report.json` and a `run-manifest.json` differing only in the copied path of `manifest-source.json`; 4 rows, 4 slots, `answered: 0` both ways; a decoy row `meta.json` planted at `interview/01/nested/` counted nowhere. Not shown from a run directory, ordered as WO-13 part B: the refusal terminal lines, an interview on a crowded row (whose session file is `loop-session-<uuid>.json`, a different name from the fresh row's), and a budget stop between questions.

**Sessions recorded before 7a26eb2 carry tool names only (ruled 2026-09-24, spec session).** The scored turn's session file recorded `offered_tools` as names, never the descriptions and schemas the model saw, so an interview on such a row mounts the fresh server's definitions after checking the names match and marks `tool_definitions_source` as unverifiable, on the terminal and in its meta. This changes nothing about how an interview is used — its answer is a claim under S21 either way — and is recorded so that a reader of an interview on an older row knows the model may have been shown tool descriptions the scored turn did not see. Sessions from 7a26eb2 on record `tool_definitions` and the interview replays them.

## Cells

A cell is identified by (driver, model, knobs, tool surface, context condition, setup, env, prompt selection), plus any identity components the driver's contract in `50-drivers.md` adds (the loop driver adds scaffold version and provider pin — S11). **`env` joined the tuple on 2026-09-21** (spec session; prompted by the uscode-mcp spec session's A/B arms, which are cells identical except for one server environment variable): S5 names `setup` and `env` as the two channels by which state reaches the server, `setup` was already in the tuple, and leaving `env` out was an omission, never a position — two cells whose servers run under different environments are measuring different servers. Secret-valued entries join identity by key name only, since the value may appear in no artifact. Observed the same day, from `runs/2026-09-18-wo3-examples/length/loop-cell/A/A1/meta.json`: the recorded `cell_id` string (`loop/fake/model-1/fresh/full/scaffold:loop-scaffold@1/pin:fakeprov/bf16`) carries neither knobs, setup, nor env, so it under-identifies cells today and rows are told apart by the manifest cell name plus the manifest hash; conformance is ordered in WO-6. The grid grows only when a question needs a new cell. Four roles recur and are recommended as the starting grid, but the roster is the suite's choice:

| role | what it isolates | typical shape |
|---|---|---|
| floor | the merge-relevant result: what a distracted mid-task consumer does | mid-tier model, no extended thinking, crowded context, full tool surface |
| ceiling | whether the data supports a correct answer at all | top model, high thinking, fresh context, question first |
| capability-floor | weakest-consumer behavior without conflating chaining limits with tool defects | small model, single-step prompt variants only |
| isolation | tool-selection noise vs tool-design defects | surface restricted to exactly the tools under test |

Attribution-dependent conclusions (e.g. "citation absent from the trace") hold **only** in cells where the trace scope equals the tool surface — the isolation role exists for that.

Cross-vendor cells never silently substitute for a gating cell of the primary vendor: a cross-vendor cell carries a distinct role and is non-gating unless the suite explicitly rules otherwise.

## Grounding rules for manifests

Adopted whole from the ancestor suite, where writing prompts from plausibility instead of the record invalidated three of them (A3, B3, E3 — see the example's annotations):

- Every prompt asserting a property of the server's data cites its grounding: a named, dated, reproducible measurement, or an observation recorded in the suite's spec.
- **Live-upstream staleness**: when the server fronts a live upstream, groundings are pinned against a stated snapshot (edition, date, content hash — whatever the domain offers). When the upstream moves, every grounding that depends on it is stale: re-measure before scoring any run against it. The per-cell meta's record of upstream identifiers actually hit is what makes staleness detectable.
- **Real-user prompts** (the ancestor's Group F): prompts meant to represent real use must be verbatim questions from real sessions, authored by no one who knows the server's internals — the suite's spec session is disqualified by construction. Every prompt carries a `sourcing` field; anything not `verbatim-original` is scored as indicative only, never as a measurement. A prompt with null `pass`/`fail` must name a pinned rubric instead (`20-manifest.md`).
